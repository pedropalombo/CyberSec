> ! PS: need to access something? Try looking for the env's global variables ($MAIL, $HOME, etc.)

> Bootloader
	^-> guides the booting process that starts the OS

 > OS Kernel
	 ^-> manages resources for I/O devices at hardware level
> 		
> Daemons
	^-> background services (scheduling, printing, multimedia, etc)
> 		#- run after boot/login
> 			
> OS Shell
	^-> interface OS<->user (command line)
> 		#- PS1 file: enables the display of system info (<username>@<hostname>[~]$) 		
> Graphics server
	^-> enables graphical programs to run (usually named "X"-server)
	
> Window Manager
	 ^-> also known as GUIs (graphical user interface)
 	 	
> Utilities/apps
	^-> perform specific functions for the user/another program
